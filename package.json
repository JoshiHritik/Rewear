{
  "name": "rewear",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w frontend && node -e \"try { require('fs').cpSync('dist', 'frontend/dist', { recursive: true }); } catch (e) {}\"",
    "vercel-build": "npm run build -w frontend && node -e \"try { require('fs').cpSync('dist', 'frontend/dist', { recursive: true }); } catch (e) {}\"",
    "seed": "npm run seed -w backend",
    "setup": "npm install && npm run seed -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "allowScripts": {
    "esbuild": true
  }
}